//10. Ordena alfabéticamente el siguiente array (en orden descendente)según el estándar UNICODE e imprime el resultado en la consola.

let colores = ["azul", "verde", "rosa", "naranja", "rojo","marron"];
colores.sort();
colores.reverse();
console.log (colores);
